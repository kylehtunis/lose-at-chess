{
  "name": "@lose-at-chess/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "check": "wrangler types && tsc --noEmit",
    "test": "vitest run",
    "types": "wrangler types"
  },
  "dependencies": {
    "@lose-at-chess/protocol": "*",
    "chess.js": "^1.4.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.129.0"
  }
}
